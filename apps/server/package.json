{
  "name": "@risk/server",
  "version": "0.1.0",
  "description": "Realtime backend for Risk browser game",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@risk/game-engine": "*",
    "@risk/shared-types": "*",
    "fastify": "^5.6.1",
    "nanoid": "^5.1.6",
    "socket.io": "^4.8.1",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
