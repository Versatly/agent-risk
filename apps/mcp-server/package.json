{
  "name": "@risk/mcp-server",
  "version": "0.1.0",
  "description": "MCP bridge for Risk game agents",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.1",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
